# OBS 凭证配置示例
# 复制本文件为 config.yaml 并填入真实值。
# config.yaml 已被 .gitignore 忽略，切勿提交到 Git。

# Access Key ID
access_key: "your-access-key-id"

# Secret Access Key
secret_key: "your-secret-access-key"

# 临时安全令牌（可选，使用临时凭证时才填）
security_token: ""
